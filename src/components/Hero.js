import React from 'react';
import imgHeroMobile from '../assets/images/image-hero-mobile.png';

const Hero = () => {
  return (
    <div>
      <img className='hero' src={imgHeroMobile} alt='' />
    </div>
  );
};

export default Hero;
